default_platform(:ios)

platform :ios do
  lane :build do
    app_store_connect_api_key
    build_app(
	workspace: "Runner.xcworkspace",
	scheme: "Runner",
	export_options: {
		method: "app-store",
	}
    )
  end
end
